{{- define "secret" -}}{{- $seed := index . 0 -}}{{- $id := index . 1 -}}{{- printf "%s:%s" $seed $id | sha256sum | trunc 48 -}}{{- end -}}
auth:
  password: {{ template "secret" (list .Values.secretSeed "redis") }}

architecture: standalone

master:
  persistence:
    size: 1Gi
