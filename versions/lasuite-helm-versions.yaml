# La Suite numerique chart versions
laSuiteChartVersions:
  docs: "5.2.1"
  meet: "0.0.23"
  drive: "0.18.0"
  people: "0.0.7"
  conversations: "0.0.7"
  find: "0.0.3"

# Image version overrides
# By default, image tags are derived from chart versions (v{laSuiteChartVersions.X}).
# Only specify here when chart version and image version differ.
# See docs/known-limitations.md for details.
laSuiteImageVersions:
  meet: "v1.19.0"  # Meet chart 0.0.x does not match app version v1.x
